import { ValidConstraintValidator } from './ValidConstraintValidator';

describe('Valid constraint validator', () => {
    let validConstraintValidator: ValidConstraintValidator<any>;

    beforeEach(() => {
        validConstraintValidator = new ValidConstraintValidator();
    });

    it.skip('todo', () => {
        // TODO
    });

});
