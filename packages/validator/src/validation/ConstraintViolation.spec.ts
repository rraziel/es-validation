import { ConstraintViolation } from './ConstraintViolation';

describe('Constraint violation', () => {
    let constraintViolation: ConstraintViolation<any>;

    beforeEach(() => {
        constraintViolation = new ConstraintViolation<any>();
    });

    it.skip('todo', () => {
        // TODO
    });

});
